version: '3.8'

services:
  open-objects:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      # Mount a volume for persistent database storage
      - open-objects-data:/app/data
    restart: unless-stopped

volumes:
  open-objects-data: